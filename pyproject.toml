[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tradingbot"
version = "0.1.0"
description = "Semi-automatic trading alert, research, and backtesting toolkit."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pandas>=2.0",
  "requests>=2.31",
  "duckdb>=1.0",
]

[project.optional-dependencies]
dev = []
research = [
  "vectorbt>=1.0.0",
]

[project.scripts]
tradingbot-monitor = "tradingbot.monitor:main"
tradingbot-scheduler = "tradingbot.scheduler:main"
tradingbot-command = "tradingbot.command:main"
tradingbot-warehouse = "tradingbot.warehouse:main"
tradingbot-backtest = "tradingbot.backtesting.run:main"

[tool.setuptools.packages.find]
include = ["tradingbot*"]
